# Quick Test Commands Reference

## 🚀 Most Common Commands

```bash
# Run all tests
pytest

# Run with coverage
pytest --cov=main --cov-report=term-missing

# Run specific test file
pytest test_main.py -v

# Run specific test class
pytest test_main.py::TestPostEndpoint -v

# Run specific test
pytest test_main.py::TestPostEndpoint::test_create_item_success -v
```

## 📊 Coverage Commands

```bash
# Coverage with missing lines
pytest --cov=main --cov-report=term-missing

# HTML coverage report
pytest --cov=main --cov-report=html
# Then: open htmlcov/index.html

# Coverage with branch analysis
pytest --cov=main --cov-branch

# Fail if coverage below 95%
pytest --cov=main --cov-fail-under=95
```

## 🔍 Filtering Tests

```bash
# Run tests matching keyword
pytest -k "delete" -v

# Run tests NOT matching keyword
pytest -k "not delete" -v

# Run tests from multiple keywords
pytest -k "delete or update" -v
```

## 🐛 Debugging Commands

```bash
# Stop on first failure
pytest -x

# Show local variables on failure
pytest -l

# More verbose output
pytest -vv

# Show print statements
pytest -s

# Drop into debugger on failure
pytest --pdb
```

## 📈 Output Options

```bash
# Quiet mode (less output)
pytest -q

# Show test durations
pytest --durations=10

# Show slowest tests
pytest --durations=0

# Generate JUnit XML report
pytest --junit-xml=report.xml
```

## 🎯 Quick Checks

```bash
# Run only failed tests from last run
pytest --lf

# Run failed tests first, then others
pytest --ff

# Run tests in parallel (requires pytest-xdist)
pytest -n auto
```

## 📝 Current Test Stats

- **Total Tests**: 29
- **Coverage**: 98.21%
- **Execution Time**: ~0.33s
- **Status**: All Passing ✅

